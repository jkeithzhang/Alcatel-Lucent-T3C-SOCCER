// 
// Right now env is only for localhost (DEV only)
// 
var server = {
	host_port: 'http://localhost:2000'
	// user: 'root',
	// password: 'tigris',
	// database: 'tshelp',
	// insecureAuth: true
};

var php_path = '/usr/bin/';