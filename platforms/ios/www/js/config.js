// 
// Right now env is only for localhost (DEV only)
// 
var server = {
	port: '8888',
	host: 'localhost',
	user: 'root',
	password: 'tigris',
	database: 'tshelp',
	insecureAuth: true
};

var php_path = '/usr/bin/';